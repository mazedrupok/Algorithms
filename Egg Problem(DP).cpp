#include <bits/stdc++.h>

#define MAX(a, b) 					 ((a > b) ? a : b)
#define MIN(a, b) 					 ((a < b) ? a : b)
#define fill(a, v)                   memset(a, v, sizeof a)
#define scf(n) 						 scanf("%d", &n)
#define pri(i) 						 printf("%i", i)

using namespace std;

int mem[100][100];

int dp(int E, int F){
	if(E == 1)
}

int main(){
	return 0;
}

